<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cl.dao.FavoritesDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.cl.entity.FavoritesEntity" id="storeupMap">
        <result property="refid" column="refid"/>
        <result property="tablename" column="tablename"/>
        <result property="name" column="name"/>
        <result property="picture" column="picture"/>
        <result property="userid" column="userid"/>
    </resultMap>

    <select id="selectListView"
		resultType="com.cl.entity.view.FavoritesView" >

		SELECT  storeup.* FROM storeup  storeup 	        
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.cl.entity.view.FavoritesView" >
		SELECT * FROM storeup  storeup <where> 1=1 ${ew.sqlSegment}</where>
	</select>



</mapper>
